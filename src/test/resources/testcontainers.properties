# Testcontainers configuration
# Note: For Rancher Desktop users, set DOCKER_HOST=unix://$HOME/.rd/docker.sock
# Disable Ryuk (resource reaper) to avoid socket mount issues with non-standard Docker setups
ryuk.disabled=true
