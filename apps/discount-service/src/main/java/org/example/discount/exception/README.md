# exception

## Purpose

Custom exceptions for discount validation and markdown authorization failures.

## Behavior

InvalidDiscountException signals promo code validation failures (404). UnauthorizedMarkdownException signals employee permission failures (403).
