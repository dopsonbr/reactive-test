# org.example.discount

## Purpose

Root package containing the Spring Boot application entry point for discount pricing engine.

## Behavior

Scans platform libraries for cross-cutting concerns (logging, error handling) and exposes discount, markdown, and pricing REST APIs via subpackages.
