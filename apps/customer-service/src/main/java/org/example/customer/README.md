# org.example.customer

## Purpose

Root package containing the Spring Boot application entry point for customer service.

## Behavior

Scans platform libraries for cross-cutting concerns (logging, error handling, security) and exposes customer REST API via subpackages. Supports both individual and business customer types with B2B hierarchy.
