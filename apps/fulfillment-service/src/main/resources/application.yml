spring:
  application:
    name: fulfillment-service

server:
  port: 8085

# Logging
logging:
  file:
    name: logs/fulfillment-service.log

# Actuator & Management
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always
