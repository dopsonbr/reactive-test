spring:
  application:
    name: customer-service

server:
  port: 8083

# Logging
logging:
  file:
    name: logs/customer-service.log

# Actuator & Management
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always
