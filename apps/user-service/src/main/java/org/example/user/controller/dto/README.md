# controller.dto

## Purpose
Data transfer objects for user service REST API requests and responses.

## Behavior
Immutable records with validation constraints for user operations, preferences, and JWT token exchange.
